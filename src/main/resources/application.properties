spring.application.name=tinycare

# MySQL DB Config
spring.datasource.url=jdbc:mysql://localhost:3306/tinycare
spring.datasource.username=root
spring.datasource.password=9893

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Server Port (optional)
server.port=8282

# Timezone (optional for consistency)
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# JWT secret (for future use)
jwt.secret=V{aPW:CLa$bG*dLw0GHbQA[DnC,9az
